# AI Chat Hub — 技术 PoC 范围

## PoC 要回答的问题

PoC 不追求完整产品，而是先证明：在不使用默认 API、不导出 Cookie、不修改官方回复的前提下，Mac 本地统一窗口能否可靠地连接两个官方网页会话，并保存可核验的原始结果。

## 推荐验证路径

采用“本地壳层 + 官方浏览器上下文 + 平台适配器”的薄层方案作为第一条路线：

```text
AI Chat Hub 本地窗口
  ├─ 工作区与并排结果视图
  ├─ 本地任务协调器（不做模型路由或 Consensus）
  ├─ Provider Adapter A ── 官方浏览器会话 A
  ├─ Provider Adapter B ── 官方浏览器会话 B
  └─ 本地 SQLite ── 原文、来源、状态和索引（不存 Cookie）
```

浏览器登录由用户在官方页面完成。适配器只获得完成 PoC 所需的最小页面交互权限；浏览器 Profile、Cookie 和凭据不复制进项目目录。Open WebUI 路线暂时只做书面评估，不在首轮直接 fork，避免在关键接入方式未验证前承担大规模派生和升级成本。

## In Scope

1. **两个平台的纵向切片**：先用 Adapter A / Adapter B 占位；实施前根据官方条款、页面可自动化程度和用户现有订阅选择具体平台。
2. **用户发起、显式确认的发送**：同一提示可分别发送；PoC 不做后台批量提交、无人值守抓取或自动重试风暴。
3. **原始回复捕获**：记录官方页面可见的文本/Markdown、平台名、官方会话 URL、时间、完成状态和采集版本。
4. **保真展示**：各平台结果独立卡片或并排呈现；原文只读，复制内容默认等于保存的原文。
5. **本地持久化**：独立 SQLite 数据库；提供最小 schema 与迁移机制，不写入 Cookie、密码、token 或浏览器 Profile。
6. **失败可见性**：覆盖未登录、验证码/安全挑战、页面结构变化、流式回复中断、超时和回复截断。
7. **本地检索与回链**：按平台、时间和关键词检索，并可打开原始官方会话进行核验。

## Out of Scope

- AI Council 的角色、路由、投票、审议或 Consensus。
- 对原始回复自动总结、评分、改写、翻译、合并或选出“最佳答案”。
- 默认 API 接入、云端代理、远程 Cookie Vault 或跨设备凭据同步。
- 绕过 CAPTCHA、MFA、反自动化机制、付费墙或平台限制。
- 兼容所有模型平台、移动端、多人协作、云同步与正式发布安装包。
- 复用现有 AI Council / Open WebUI 的数据库、配置目录或运行实例。

## 分阶段实验

### P0 — 合规与技术门槛

- 为候选平台记录官方条款、允许的交互方式和自动化风险。
- 验证可在用户可见的本地浏览器上下文中完成登录与会话跳转。
- 确认浏览器数据目录在项目外，日志经过敏感信息过滤。

**停止条件**：需要导出 Cookie、规避平台控制，或无法在用户可见且可撤销的授权下工作。

### P1 — 单平台保真链路

- 手动确认后发送一条提示，等待流式回复结束。
- 捕获原文及来源元数据，写入临时本地数据库。
- 将保存内容与官方页面逐字符或规范化后对比，并标记动态 UI 元素。

**通过标准**：连续 20 次正常会话无串会话、误发送或静默截断；失败都能被识别。

### P2 — 双平台统一窗口

- 同一提示分别提交给两个平台，保持两个独立任务状态。
- 并排展示原始回复、来源、完成状态和官方回链。
- 单个平台失败时不污染另一个平台的记录。

**通过标准**：连续 10 组双平台测试中，来源归属、文本、状态和回链全部正确。

### P3 — 本地恢复与隐私检查

- 重启应用后恢复历史；断网时只读查看既有内容。
- 检查数据库、日志、崩溃报告和 Git 状态中没有凭据或 Cookie。
- 模拟登录过期和页面选择器失效，确认安全失败。

**通过标准**：恢复结果一致；敏感信息扫描无发现；故障不触发误发送。

## 建议的 PoC 交付物

- 两个平台适配器的最小接口与风险记录。
- 本地数据模型及不可变原文策略。
- 可运行的 Mac 本地演示壳层。
- 20 次单平台、10 组双平台的测试记录。
- Go / Revise / Stop 决策：继续自建薄层，评估独立 Open WebUI 派生，或停止网页自动化路线。

## 首轮决策门

在写平台适配代码前，需要确定首批两个官方平台，并逐一核对其当前服务条款与浏览器自动化边界。这个决定会影响技术栈、可维护性和项目能否对外分发，因此不在目录初始化阶段替用户假定。
