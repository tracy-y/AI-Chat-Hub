# AI Chat Hub — 创建、开发、验收全流程

> 状态说明：`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成，`[!]` 阻塞。

> 执行方式：这是导航清单，不是行政审批表。个人本地开发采用精简流程；只有影响数据安全、账号权限、平台条款、不可逆迁移或公开发布的事项才要求额外文档和人工确认。

## 总体完成定义

只有同时满足以下条件，项目才算完成首个正式版本：

- 两个经过确认的官方平台可以从统一窗口分别提交问题。
- 两个平台的官方原始回复均能完整、独立、可核验地显示和保存。
- 不修改原始回复，不混淆来源，不静默隐藏失败或截断。
- 官方账号、登录和 Cookie 留在 Mac 本地浏览器上下文。
- API 不是默认接入方式；AI Council 的角色、路由和 Consensus 未被复用。
- 项目代码、配置、数据库、运行环境和 Git 仓库均保持独立。
- 功能、保真、隐私、安全、恢复和安装验收全部通过。
- 用户完成最终 UAT，并明确批准发布。

---

## Phase 0 — 项目创建与治理基线

### Checklist

- [x] 创建独立项目目录 `official-ai-chat-hub`。
- [x] 建立 Obsidian `Codex 输出`符号链接。
- [x] 初始化独立 Git 仓库和 `main` 分支。
- [x] 连接 GitHub 仓库 `tracy-y/AI-Chat-Hub`。
- [x] 创建产品定义、PoC 范围和独立性 ADR。
- [x] 创建基础职责目录和 `.gitignore`。
- [x] 完成首次 commit 和 push。
- [ ] 公开发布前再决定是否为 `main` 开启分支保护。
- [x] 版本规则：PoC 使用 `0.x`，首个正式版使用 `1.0.0`。

### 工作规则

- 一组可独立验收的功能使用一个 `codex/<topic>` 分支；微小文档修正不强制新建分支。
- Codex 可以修改、检查并直接创建本地 commit；push、合并和发布仍由用户确认。
- 合并、发布、安装到正式位置或修改官方账号状态前，必须由用户确认。
- 检查强度与风险相称；涉及凭据、原文、存储或迁移时必须执行完整检查。
- AI Council 目录始终只读且不纳入本项目工具链。
- 小改动不要求 Issue、PR、ADR、changelog 或单独测试报告。

### 阶段出口

- [x] 精简 Git 工作流和不可省略的安全边界已写入 `CONTRIBUTING.md`。

---

## Phase 1 — 产品范围冻结

### 用户决策

- [ ] 确定首批两个官方平台。
- [x] 确定发行模式：开发者本人本地使用，成熟后通过 GitHub 公开分发。
- [x] 确定账号模式：每位用户自行注册、登录并使用自己的官方订阅账号。
- [ ] 确定首版支持的 Mac 型号和最低 macOS 版本。
- [ ] 确定是否允许“同一问题一次确认、分别发送到两个平台”。
- [ ] 确定本地历史默认保留期限以及一键删除需求。
- [ ] 确定是否需要附件；建议首版只做纯文本。
- [ ] 选择开源许可证，并检查所有第三方依赖与派生代码的许可证兼容性。

### 必要产品记录

- [ ] 只记录首版 Must 功能、明确非目标和关键用户流程。
- [ ] 只对凭据、原始回复、日志和导出数据保留数据分类说明。
- [ ] 可访问性、语言和外观要求直接进入实现清单，不另建报告。

### 阶段出口

- [ ] 首版范围不存在未决定的 Must 项。
- [ ] 产品定义由用户确认冻结；新增范围进入后续版本，不中途扩张。

---

## Phase 2 — 平台合规与接入可行性（P0）

每个平台单独建立一份 `docs/providers/<provider>-assessment.md`。

当前结论：Claude 消费订阅网页自动化为 `Stop`；ChatGPT 消费订阅网页自动化为 `Revise / Unconfirmed`。用户已选择手动辅助转接作为默认路线；可选 API 不进入默认路径。

### 每个平台检查项

- [ ] 阅读并记录当前服务条款、使用政策和自动化相关限制。
- [ ] 区分“个人本地辅助操作”和“自动化抓取/批量使用”的风险。
- [ ] 确认不会绕过 CAPTCHA、MFA、速率限制、付费墙或安全控制。
- [ ] 确认登录由用户在可见的官方页面中完成。
- [ ] 验证是否能复用浏览器会话而不导出 Cookie。
- [ ] 验证新建会话、发送、等待完成、读取回复和回到原会话的技术可行性。
- [ ] 记录流式回复、引用、代码块、表格和错误消息的页面表现。
- [ ] 记录页面改版、A/B 测试、地区差异和账号差异带来的维护风险。
- [ ] 为平台给出 `Go / Revise / Stop` 结论。

### 停止条件

- [ ] 若必须导出 Cookie、模拟绕过安全机制或违反明确的平台限制，则停止该平台路线。
- [ ] 若无法可靠识别回复完成或无法确认来源，则不进入正式开发。
- [ ] 若只能通过 API 合规实现，则作为可选 API 适配器重新评估，不改变订阅优先原则。

### 阶段出口

- [ ] 至少两个平台获得明确的 PoC `Go` 结论。
- [ ] 用户确认接受已记录的稳定性、条款和维护风险。

---

## Phase 3 — 技术路线与架构冻结

### 候选路线评估

- [ ] 原生/轻量桌面壳层 + 用户可见浏览器控制。
- [ ] 浏览器扩展 + 本地 companion app。
- [ ] Electron 或 Tauri 内嵌/外接浏览器方案。
- [ ] Open WebUI 独立派生方案，仅评估 UI、许可证、升级和数据隔离成本。

### 只为高影响决策保留 ADR

- [ ] 浏览器控制方式及账号、Cookie 权限边界。
- [ ] 持久化、迁移与不可变原文策略。
- [ ] 影响公开分发的技术栈、许可证或更新机制。

Provider Adapter、小型 UI 和普通实现细节直接通过代码、测试和简短注释表达，不单独写 ADR。

### 核心数据模型

- [ ] `Workspace`：本地工作区。
- [ ] `PromptSubmission`：用户确认过的提交任务。
- [ ] `ProviderRun`：某个平台的独立执行状态。
- [ ] `OfficialConversationRef`：官方平台与原会话回链。
- [ ] `ImmutableResponse`：不可覆盖的官方原文与采集元数据。
- [ ] `CaptureEvidence`：完成状态、适配器版本与校验信息。
- [ ] `UserAnnotation`：与原文分层保存的用户标签或笔记。

### 阶段出口

- [ ] 架构图、接口草案、schema 和威胁边界完成评审。
- [ ] 关键技术路线有小实验支持，不只依据主观偏好。

---

## Phase 4 — UX 原型与交互验收

### 原型页面

- [ ] 首次启动与隐私说明。
- [ ] 平台连接状态与“打开官方登录页”。
- [ ] 统一输入区和发送前确认。
- [ ] 双平台独立进度状态。
- [ ] 并排原始回复视图。
- [ ] 失败、未登录、验证码、超时和截断状态。
- [ ] 历史记录、搜索、筛选和官方回链。
- [ ] 本地数据管理、导出与删除。

### 交互原则验收

- [ ] 每份回复始终显示平台、会话和时间。
- [ ] 用户能区分“正在生成”“已完成”“可能截断”“失败”。
- [ ] 复制原文不经过摘要、格式改写或模型处理。
- [ ] 单个平台失败不会阻塞或覆盖另一个平台。
- [ ] 发送、删除历史、清空数据等动作有合适的确认。

### 阶段出口

- [ ] 用户走完完整原型流程并确认布局与状态表达。

---

## Phase 5 — 工程基础设施

### 项目基础

- [ ] 选择并锁定运行时、包管理器和版本。
- [ ] 建立开发、测试和生产配置分层。
- [ ] 创建代码格式、lint、类型检查和单元测试命令。
- [ ] 创建数据库迁移与测试数据库工具。
- [ ] 建立统一错误模型和结构化日志。
- [ ] 默认关闭遥测，日志自动过滤凭据和会话敏感字段。
- [ ] 建立本地 fixture；测试不得依赖真实 Cookie 或真实私人对话。

### CI（公开发布前完成）

- [ ] GitHub Actions 执行格式、lint、类型检查、单元测试和构建。
- [ ] 添加依赖漏洞和凭据扫描。
- [ ] CI 不接触真实平台账号、Cookie 或本机数据库。
- [ ] 所有必需检查通过后才允许合并。

### 阶段出口

- [ ] 空壳应用可在干净环境构建和启动。
- [ ] PoC 阶段本地检查通过；公开发布前再要求 CI 与本地结果一致。

---

## Phase 6 — 单平台纵向 PoC（P1）

先完成 Adapter A，证明整条链路，再复制接口经验到第二个平台。

### 功能实现

- [ ] 检测登录状态，但不读取或保存 Cookie 内容。
- [ ] 用户确认后创建或选择官方会话。
- [ ] 输入并提交纯文本提示。
- [ ] 观察流式生成并可靠判断完成。
- [ ] 捕获官方可见原文和必要来源信息。
- [ ] 将原文写入独立 SQLite 数据库。
- [ ] 提供“打开官方会话”和“复制原文”。
- [ ] 对登录过期、挑战页面、页面改版和超时安全失败。

### P1 验收

- [ ] 连续完成 20 次正常会话。
- [ ] 20 次均无误发、串会话、静默截断或来源错误。
- [ ] 保存内容与官方页面逐字符或按已定义规范化规则一致。
- [ ] 关闭并重启后记录仍可读取。
- [ ] 数据库、日志和 Git 扫描无 Cookie、密码或 token。

### 阶段出口

- [ ] Adapter A 达到 P1 全部通过标准。
- [ ] 将选择器、完成判定和失败模式整理成可复用接口，而非复制临时代码。

---

## Phase 7 — 第二平台与统一适配器契约（P2）

### 功能实现

- [ ] 使用同一 adapter contract 实现 Adapter B。
- [ ] 两个平台保持独立浏览器上下文、任务状态和会话引用。
- [ ] 同一提示由用户一次确认后分别提交。
- [ ] 并排展示两份独立原始回复。
- [ ] 任何失败只影响对应的 `ProviderRun`。
- [ ] 支持取消尚未发送或仍在执行的单个平台任务。
- [ ] 为每个平台记录适配器版本和页面兼容信息。

### P2 验收

- [ ] 连续完成 10 组双平台会话。
- [ ] 20 份回复的平台归属、原文、状态和回链全部正确。
- [ ] 模拟 A 失败时 B 正常完成；再反向测试。
- [ ] 并发操作无重复发送、交叉写入和状态覆盖。

### 阶段出口

- [ ] 双平台核心能力达到 PoC 成功定义。
- [ ] 形成 `Go / Revise / Stop` PoC 总结，由用户批准进入 MVP。

---

## Phase 8 — MVP 产品化

### 核心功能

- [ ] 工作区和对话历史。
- [ ] 按平台、日期、关键词和状态检索。
- [ ] 原始回复只读保护。
- [ ] 用户标签与笔记独立存储。
- [ ] 单条或选中记录导出。
- [ ] 清晰的平台连接/失效状态。
- [ ] 数据库 schema migration 和备份恢复。
- [ ] 应用设置、数据目录说明和隐私控制。

### 暂不进入 MVP

- [ ] 自动总结、评分、翻译或答案合并。
- [ ] 无人值守批量任务。
- [ ] 云同步、多人账号或远程 Cookie 管理。
- [ ] AI Council 风格的角色、路由和 Consensus。
- [ ] 默认模型 API。

### 阶段出口

- [ ] 所有 Must 用户故事完成。
- [ ] 数据迁移、失败恢复和卸载边界明确。

---

## Phase 9 — 隐私、安全与故障恢复

### 威胁检查

- [ ] 绘制数据流和信任边界。
- [ ] 检查 Cookie、Session、密码和 token 是否可能进入日志、数据库或崩溃报告。
- [ ] 检查页面内容是否可能触发本地命令、脚本或路径访问。
- [ ] 限制浏览器控制权限到必要域名和操作。
- [ ] 防止跨平台会话、窗口句柄或数据库记录混淆。
- [ ] 检查导出文件中的隐私提示和默认保存位置。
- [ ] 检查依赖、供应链、自动更新和签名风险。

### 恢复测试

- [ ] 应用强制退出后恢复。
- [ ] Mac 重启后恢复。
- [ ] 生成中断网后恢复或明确失败。
- [ ] 数据库被占用、迁移失败或磁盘空间不足时安全失败。
- [ ] 登录过期后不循环发送、不重复提交。
- [ ] 页面选择器失效后不捕获错误区域并标成成功。
- [ ] 备份恢复不会覆盖更新的数据，除非用户明确确认。

### 阶段出口

- [ ] 无未处理的高风险问题。
- [ ] 所有敏感数据测试与故障注入测试有留存证据。

---

## Phase 10 — 完整验收

### 功能验收

- [ ] 首次启动、连接平台、发送、查看、搜索、回链、导出和删除均通过。
- [ ] 两个平台可以独立启用、禁用和恢复。
- [ ] 所有用户可见状态与真实执行状态一致。

### 保真验收

- [ ] 普通文本、Markdown、代码块、列表、表格、引用和特殊字符均正确。
- [ ] 长回复、流式回复、重新生成和中断回复均正确标记。
- [ ] 原文不可被用户笔记或未来处理结果覆盖。

### 隐私验收

- [ ] Cookie、密码、token 不出现在 Git、日志、数据库、导出或崩溃报告中。
- [ ] 断网时历史记录仍可只读访问。
- [ ] 用户能够定位并删除本地应用数据。

### 兼容与体验验收

- [ ] 在最低和当前目标 macOS 版本测试。
- [ ] 浅色、深色、窗口缩放和常见分辨率测试。
- [ ] 中文、英文、Emoji 和超长文本测试。
- [ ] 键盘操作、焦点顺序和基本可访问性测试。

### 回归门槛

- [ ] 单元测试、集成测试、验收测试全部通过。
- [ ] `git diff --check`、lint、类型检查、构建和凭据扫描通过。
- [ ] 没有 P0/P1 缺陷；P2 缺陷有明确接受或延期决定。
- [ ] 用户完成真实账号的 UAT，且测试内容可控、不包含不必要隐私数据。

---

## Phase 11 — 发布候选版与正式发布

### Release Candidate

- [ ] 冻结功能，只修复发布阻塞问题。
- [ ] 更新 README、安装、隐私、备份、卸载和故障排除文档。
- [ ] 生成版本号、changelog 和已知问题清单。
- [ ] 从干净 checkout 完成可重复构建。
- [ ] 对构建产物做 checksum、签名/notarization（若采用正式分发）。
- [ ] 在非开发环境进行全新安装与升级测试。
- [ ] 用户明确批准 RC 进入正式发布。

### 正式发布

- [ ] 创建 release commit 和 Git tag。
- [ ] 用户手动 push 分支与 tag。
- [ ] 发布 GitHub Release 和安装包。
- [ ] 下载已发布产物重新核验 checksum、启动和版本号。
- [ ] 记录可回滚版本和恢复方法。

### 阶段出口

- [ ] 已发布产物通过独立安装后的冒烟测试，而不是只验证开发目录。

---

## Phase 12 — 上线后维护

### 日常维护

- [ ] 监控平台页面变化，但不收集用户聊天内容或凭据遥测。
- [ ] 为每个平台维护兼容矩阵和最后验证日期。
- [ ] 页面变化时先禁用不安全操作，再修复适配器。
- [ ] 每次依赖升级执行完整回归和敏感信息扫描。
- [ ] 定期验证数据库迁移、备份和恢复。
- [ ] 定期复核平台条款与分发边界。

### 版本节奏

- [ ] Patch：缺陷、安全和平台兼容修复。
- [ ] Minor：新增平台或非破坏性功能。
- [ ] Major：数据库、接入模式或隐私边界发生重大变化。

### 新平台准入模板

- [ ] 完成独立条款和风险评估。
- [ ] 实现 adapter contract。
- [ ] 完成 20 次单平台保真测试。
- [ ] 完成故障、隐私和回归测试。
- [ ] 用户批准后才进入默认可用列表。

---

## 精简开发循环

普通功能默认按以下顺序执行，不要求先建 Issue 或 PR：

1. [ ] 用一两句话确定目标和可验证结果。
2. [ ] 检查工作区，完成最小可运行改动。
3. [ ] 按风险运行相关测试；涉及安全边界时运行完整验证和凭据扫描。
4. [ ] Codex检查 diff 并直接创建本地 commit。
5. [ ] Codex简要汇报结果、风险和需要用户实际操作的验收步骤。
6. [ ] 用户确认后再 push、合并或发布。

只有数据库破坏性迁移、真实账号权限、平台条款、高风险安全改动和正式发布需要更完整的方案或证据记录。

---

## 当前下一步

1. [x] 首批评估平台选择为 ChatGPT 和 Claude。
2. [x] 明确项目先供本人本地使用，成熟后通过 GitHub 公开分发。
3. [x] 明确每位用户使用自己的官方账号和订阅，不提供共享凭据或代理账号。
4. [x] 已完成公开资料 P0，并选择手动辅助转接作为安全接入路线。
5. [ ] 评估完成后再冻结正式技术栈；工程 PoC 优先验证浏览器扩展侧边栏路线。
