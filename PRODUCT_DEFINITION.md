# AI Chat Hub — 一页产品定义

## 一句话定位

AI Chat Hub 是运行在 Mac 本地的、订阅优先的多平台 AI 对话工作台：用户继续使用各平台的官方账号与官方 Agent 客户端，在一个统一窗口中发起、查看和管理对话，同时保留每个平台的原始输出。

## 目标用户与核心问题

目标用户已经订阅多个官方 AI 产品，希望减少反复切换窗口、复制问题和整理答案的成本，但不希望为了统一界面再次购买 API 用量，也不愿把账号凭据、Cookie 或聊天内容交给第三方服务器。项目首先服务于开发者本人在 Mac 本地使用，成熟后通过 GitHub 公开；其他用户必须自行注册官方 AI 账号，并使用各自账号的有效订阅。

## 核心价值

- **本地优先**：应用、配置、索引与数据库默认只存在于用户的 Mac。
- **订阅优先**：优先复用用户已购买且由官方客户端支持的订阅；API 只能是未来明确启用的可选适配器，绝不是默认接入。
- **官方回复保真**：原始回复按来源独立保存和展示，保留平台、会话、时间与采集状态；产品不改写、不拼接、不替模型下结论。
- **统一窗口**：用一致的工作区管理提问、标签、搜索、会话跳转和多平台并排查看，但不伪装成单一“综合模型”。
- **凭据不出本机**：登录与凭据由 Mac 上的官方客户端保管；项目不收集明文密码，不把 token 或 Cookie 同步到服务端或写入 Git。

## 产品原则

1. 每条回复始终能看出“哪个官方平台、哪个会话、何时产生”。
2. 原文与任何用户主动触发的笔记、翻译或比较结果分层存储；原文不可被后处理覆盖。
3. 采集失败、内容截断、页面结构变化或登录失效必须明确提示，不能把不完整结果标成成功。
4. 用户可以随时回到对应官方页面核验上下文。
5. 默认不做云端账户系统、遥测、Cookie 上传或聊天内容云同步。

## 明确不做

- 不复用 AI Council 的角色系统、quick/full 路由、并行角色编排或 Consensus。
- 不修改、润色、摘要或合并各模型的原始回答后再冒充官方输出。
- 不以 OpenAI-compatible API 或任何模型 API 作为默认通道。
- 不绕过登录、安全校验、订阅限制、速率限制或平台访问控制。
- 不读取或迁移 AI Council 的代码、配置、数据库与 Git 历史。
- 不把 Open WebUI 的现有实例、数据库或用户数据直接拿来复用。
- 不提供共享账号、共享订阅、集中式登录代理、Cookie 托管或订阅转售。

## 首个可用版本

用户在 Mac 上启动 AI Chat Hub，对同一问题同时调用两个已登录的官方订阅 Agent；Hub 分别展示两份带清晰来源标记的原始回复并保存到本机。任何平台失败时，另一平台结果不受影响，并能看到具体失败状态。

## 成功指标

- 两个平台从提交到展示的完整链路可重复运行。
- 保存内容与官方 Agent 返回内容一致，且能检测失败或空结果。
- 登录凭据不离开官方客户端的本机存储，不进入应用数据库、日志或 Git。
- 断网后仍可查看已保存的原始回复与来源信息。
- 平台页面改版或登录失效时安全失败，不误发、不串会话、不伪造成功。

## 独立性边界

本项目使用独立目录、独立 Git 仓库、独立配置、独立数据库和独立运行端口。Open WebUI 只可作为交互与信息架构参考，或在完成许可证、升级与数据隔离评估后作为独立派生起点；AI Council 现有目录保持不动。公开仓库只发布应用代码与文档，不包含任何用户的账号、会话、Cookie、聊天数据库或私人测试数据。
